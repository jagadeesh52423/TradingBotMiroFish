# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Secrets — never commit
.env
*.env

# Nubra SDK session store (shelve token cache — never commit)
auth_data.db
auth_data.db.*

# State & artifacts
state/reports/*.json
state/reports/*.md
!state/reports/.gitkeep
state/raw/
state/seeds/
state/cache/

# Internal task files
.tasks/

# macOS
.DS_Store
state/runs/
state/backtests/
